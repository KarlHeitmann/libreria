<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @category.description %>
</p>

<%= link_to 'Edit', edit_category_path(@category), class: 'btn btn-success my-1' %>
<br>
<%= link_to 'View books', category_books_path(@category), class: 'btn btn-primary my-1' %>
