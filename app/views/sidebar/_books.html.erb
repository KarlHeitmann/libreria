<nav id="sidebarMenu" class="p-0 col-md-3 col-lg-2 d-md-block bg-light sidebar collapse">
  <div class="sidebar-sticky pt-3">
    <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
      <span>Libros de <%= @category.name %></span>
      <a class="d-flex align-items-center text-muted" href="#" aria-label="Add a new report">
        <span data-feather="plus-circle"></span>
      </a>
    </h6>
    <ul class="nav flex-column">
      <% @category.books.all.each do |book| %>
        <li class="nav-item">
          <%= link_to book.title, category_book_path(@category, book), class: "nav-link" %>
        </li>
      <% end %>
    </ul>

    <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
      <span>Todos los libros</span>
      <a class="d-flex align-items-center text-muted" href="#" aria-label="Add a new report">
        <span data-feather="plus-circle"></span>
      </a>
    </h6>
    <ul class="nav flex-column">
      <% Book.all.each do |book| %>
        <li class="nav-item">
          <%= link_to book.title, category_book_path(book.category, book), class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>